@model AcadProgramViewModel

@section title
{
    @if (Model != null)
    {
        <h3>Academic Program: @Model.AcadProgram?.Name</h3>
    }
}

@if (Model != null)
{
    <partial name="_AcadProgram" model="Model" />
}

@model AcadProgramViewModel
@{
    string action = ViewBag.Action ?? string.Empty;
}

<form method="post" asp-controller="AcadPrograms" asp-action="@action">
    @if (action?.ToLower() == "edit")
    {
        <input type="hidden" asp-for="AcadProgram.ProgramId" />
    }
    <div class="text-danger" asp-validation-summary="All"></div>

    <div class="mb-3 row">
        <div class="col-2">
            <label asp-for="AcadProgram.CategoryId" class="col-form-label"></label>
        </div>

        <div class="col-6">
            <select asp-for="AcadProgram.CategoryId" class="form-control">
                <option>- Please Select -</option>
                @if (Model.Categories != null && Model.Categories.Count() > 0)
                {
                    @foreach (var cat in Model.Categories)
                    {
                        <option value="@cat.CategoryId">@cat.Name</option>
                    }
                }
            </select>
        </div>
        <div class="col">
            <span class="text-danger" asp-validation-for="AcadProgram.CategoryId"></span>
        </div>
    </div>

    <div class="mb-3 row">
        <div class="col-2">
            <label asp-for="AcadProgram.Name" class="col-form-label"></label>
        </div>
        <div class="col-6">
            <input type="text" asp-for="AcadProgram.Name" class="form-control" />
        </div>
        <div class="col">
            <span class="text-danger" asp-validation-for="AcadProgram.Name"></span>
        </div>
    </div>

    <div class="mb-3 row">
        <div class="col-2">
            <label asp-for="AcadProgram.Price" class="col-form-label"></label>
        </div>
        <div class="col-6">
            <input type="number" asp-for="AcadProgram.Price" class="form-control" min="0" step="any" />
        </div>
        <div class="col">
            <span class="text-danger" asp-validation-for="AcadProgram.Price"></span>
        </div>
    </div>

    <div class="mb-3 row">
        <div class="col-2">
            <label asp-for="AcadProgram.Quantity" class="col-form-label"></label>
        </div>
        <div class="col-6">
            <input type="number" asp-for="AcadProgram.Quantity" class="form-control" min="1" />
        </div>
        <div class="col">
            <span class="text-danger" asp-validation-for="AcadProgram.Quantity"></span>
        </div>
    </div>
    <div class="row mb-3">
        <div class="col-2">
            <input type="submit" class="btn btn-primary" value="Save" />
        </div>
        <div class="col-6">
        </div>
    </div>
</form>